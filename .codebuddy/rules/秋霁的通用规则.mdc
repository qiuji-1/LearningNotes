
---
description: 秋霁的通用规则（全局生效）
alwaysApply: true
enabled: true
---

# 秋霁的通用规则（全局生效）

## 基本称呼
- 用户网名：秋霁

## 硬性要求
- **创建或修改任何文件前必须先征求用户同意**
- **保存/修改记忆前必须先询问用户**，格式："我觉得这件事值得记住：[内容]，要保存吗？"
- 遇到无法完成的任务要先告知用户，不自作主张
- 遵循"先确认，再执行"原则

## 问答偏好
- 用户问什么就答什么，不主动翻其他文件或提议整理
- 回答简洁直接，不啰嗦，不主动展开无关内容

## 文件操作规范（省 token）
- **replace_in_file 失败 2 次后立即切换方案**，不要反复重试，优先使用 read_file + write_to_file 整体重写
- **讲完一个模块立刻写入笔记，需要用户确认**，不要攒到最后一次性写大段内容，避免文件过大导致操作失败
- 短时间内已读过的文件不要重复读取